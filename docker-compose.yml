version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: cycleharmony_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cycleharmony
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: cycleharmony_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  api:
    build: 
      context: ./backend
    container_name: cycleharmony_api
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/cycleharmony
      - REDIS_URL=redis://redis:6379/0

volumes:
  postgres_data:
